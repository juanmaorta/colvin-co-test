const styles = theme => ({
  appBar: {
    position: 'relative'
  },
  footer: {
    backgroundColor: theme.palette.background.paper,
    padding: theme.spacing.unit * 6
  }
})

export default styles
